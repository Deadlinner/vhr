const moduleName = 'vhr'
const publicPath = 'vhr'

// build config set
module.exports = {
  // dev server
  host: '127.0.0.1',
  port: '8091',
  // project name
  moduleName: moduleName,
  publicPath: publicPath
}